# 그래프 줌 컨트롤 가이드

## 개요
그래프 뷰에서 모든 노드와 엣지를 한 화면에 표시하고, 줌 레벨을 자동/수동으로 조정할 수 있는 기능이 추가되었습니다.

## 주요 기능

### 1. 📐 전체 보기 (Fit to Screen)
**기능**: 모든 노드와 엣지가 화면에 보이도록 자동으로 줌 레벨 조정

**사용 방법**:
- "📐 전체 보기" 버튼 클릭
- 800ms 애니메이션과 함께 모든 요소가 화면에 맞춰짐
- 넓은 패딩(100px)으로 여유있게 표시

**자동 실행**:
- 데이터 로드 시 자동으로 전체 보기 적용 (500ms 후)
- 물리 엔진이 멈출 때 자동으로 전체 보기 적용

### 2. 🎯 자동 정렬 (Auto Align)
**기능**: 그래프를 화면 중앙에 배치하고 카메라를 정면으로 리셋

**사용 방법**:
- "🎯 자동 정렬" 버튼 클릭
- 카메라가 정면(z축 300 거리)으로 이동
- 그래프가 중앙에 배치됨

### 3. 줌 컨트롤 (Zoom Controls)

#### ➖ 줌 아웃 (Zoom Out)
- 카메라를 25% 멀리 이동
- 줌 레벨 20% 감소
- 최소 줌 레벨: 20%

#### ➕ 줌 인 (Zoom In)
- 카메라를 20% 가까이 이동
- 줌 레벨 20% 증가
- 최대 줌 레벨: 300%

#### 🔄 줌 리셋 (Zoom Reset)
- 줌 레벨을 100%로 리셋
- 카메라를 기본 위치(z축 300)로 이동
- 800ms 부드러운 애니메이션

#### 줌 레벨 표시
- 현재 줌 레벨을 퍼센트로 표시
- 실시간 업데이트

## UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│ [3D] [2D] │ [📐 전체 보기] [🎯 자동 정렬] │ [➖] 100% [➕] [🔄] │ 노드: 300 엣지: 20,589 │
└─────────────────────────────────────────────────────────────────┘
```

### 컨트롤 그룹
1. **뷰 모드**: 3D/2D 전환
2. **화면 맞춤**: 전체 보기, 자동 정렬
3. **줌 컨트롤**: 줌 아웃, 줌 레벨 표시, 줌 인, 줌 리셋
4. **정보**: 노드 수, 엣지 수

## 버튼 스타일

### 📐 전체 보기 (보라색)
- 배경: 그라디언트 보라색 (#8b5cf6 → #7c3aed)
- 호버: 더 진한 보라색 + 글로우 효과

### 🎯 자동 정렬 (초록색)
- 배경: 그라디언트 초록색 (#10b981 → #059669)
- 호버: 더 진한 초록색 + 글로우 효과

### 🔄 줌 리셋 (주황색)
- 배경: 그라디언트 주황색 (#f59e0b → #d97706)
- 호버: 더 진한 주황색 + 글로우 효과

### 줌 컨트롤 그룹
- 배경: 반투명 파란색 (rgba(37, 99, 235, 0.1))
- 테두리: 파란색 (rgba(37, 99, 235, 0.2))
- 그룹으로 묶여 시각적으로 구분됨

## 기술 구현

### 줌 인/아웃 알고리즘
```typescript
// 줌 인: 카메라를 20% 가까이
const newDistance = currentDistance * 0.8
const ratio = newDistance / currentDistance

// 줌 아웃: 카메라를 25% 멀리
const newDistance = currentDistance * 1.25
const ratio = newDistance / currentDistance

// 카메라 위치 조정
graphRef.current.cameraPosition(
  {
    x: camera.position.x * ratio,
    y: camera.position.y * ratio,
    z: camera.position.z * ratio
  },
  { x: 0, y: 0, z: 0 },
  500 // 애니메이션 시간
)
```

### 전체 보기 알고리즘
```typescript
// zoomToFit(duration, padding)
graphRef.current.zoomToFit(800, 100)
// 800ms 애니메이션, 100px 패딩
```

### 자동 실행 타이밍
1. **데이터 로드 시**: 500ms 지연 후 전체 보기
2. **물리 엔진 정지 시**: 즉시 전체 보기

## 사용 시나리오

### 시나리오 1: 전체 그래프 탐색
1. 페이지 로드 → 자동으로 모든 노드가 화면에 표시됨
2. 특정 영역 확대 → "➕" 버튼으로 줌 인
3. 다시 전체 보기 → "📐 전체 보기" 버튼 클릭

### 시나리오 2: 특정 의원 관계 탐색
1. 의원 검색 및 선택
2. 해당 의원의 관계 그래프 로드
3. 자동으로 관계 그래프가 화면에 맞춰짐
4. 세부 관계 확인 → "➕" 버튼으로 줌 인

### 시나리오 3: 그래프 조작 후 복원
1. 노드 드래그로 그래프 조작
2. 화면 밖으로 노드 이동
3. "📐 전체 보기" 버튼으로 모든 노드 다시 표시
4. "🔄" 버튼으로 줌 레벨 리셋

## 키보드 단축키 (향후 추가 가능)
- `+` 또는 `=`: 줌 인
- `-`: 줌 아웃
- `0`: 줌 리셋
- `F`: 전체 보기 (Fit to screen)
- `C`: 자동 정렬 (Center)

## 성능 최적화
- 줌 애니메이션: 500ms (부드러운 전환)
- 전체 보기 애니메이션: 800ms (여유있는 전환)
- 줌 리셋 애니메이션: 800ms
- 상태 업데이트: React useState로 효율적 관리

## 브라우저 호환성
- Chrome/Edge: ✅ 완벽 지원
- Firefox: ✅ 완벽 지원
- Safari: ✅ 완벽 지원
- 모바일 브라우저: ✅ 터치 제스처와 함께 사용 가능

## 문제 해결

### 문제: 줌 레벨이 정확하지 않음
**해결**: 줌 레벨은 상대적 표시이며, 실제 카메라 거리와 정확히 일치하지 않을 수 있습니다. "🔄" 버튼으로 리셋하세요.

### 문제: 전체 보기가 작동하지 않음
**해결**: 그래프 데이터가 로드되었는지 확인하세요. 노드 수가 0이면 작동하지 않습니다.

### 문제: 애니메이션이 끊김
**해결**: 브라우저 하드웨어 가속을 활성화하거나, 다른 탭을 닫아 메모리를 확보하세요.

## 파일 변경사항

### 수정된 파일
- `KoreaPolitician/frontend/src/components/GraphVisualization.tsx`
  - `handleFitToScreen()` 함수 추가
  - `handleZoomIn()` 함수 추가
  - `handleZoomOut()` 함수 추가
  - `handleZoomReset()` 함수 추가
  - `zoomLevel` 상태 추가
  - UI 컨트롤 버튼 추가

- `KoreaPolitician/frontend/src/components/GraphVisualization.css`
  - `.control-divider` 스타일 추가
  - `.control-btn.fit-btn` 스타일 추가
  - `.zoom-controls` 스타일 추가
  - `.control-btn.zoom-btn` 스타일 추가
  - `.control-btn.zoom-reset-btn` 스타일 추가
  - `.zoom-level` 스타일 추가

## 다음 단계 (향후 개선)
1. 키보드 단축키 지원
2. 마우스 휠 줌 감도 조정
3. 줌 레벨 슬라이더 추가
4. 미니맵 추가 (전체 그래프 미리보기)
5. 북마크 기능 (특정 줌/위치 저장)
